# Nix
result
result-*
.direnv/

# Rust / Cargo
target/
ext/tictactoe/target/
# Cargo.lock is committed for reproducibility

# Ruby / Bundler
*.gem
*.rbc
.bundle/
vendor/bundle/
# gemset.nix and Gemfile.lock are INTENTIONALLY committed (Nix lockfiles)
# !Gemfile.lock
# !gemset.nix

# Compiled extension
lib/tictactoe/tictactoe.{so,bundle,dll}
tmp/

# Editor
.idea/
.vscode/
*.swp
*.swo
.DS_Store
lib/tictactoe/tictactoe.so
